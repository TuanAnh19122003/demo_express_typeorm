<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <h1>
        Home
    </h1>
    <a href="/logout">Logout</a>
    <h2>
        Xin chào: <%= session.userLogin.firstName + " " + session.userLogin.lastName %>
    </h2>
    <% if(session.userLogin.role.id == 1){ %>
        <a href="/weather">Weather</a>
    <% } %>
</body>
</html>